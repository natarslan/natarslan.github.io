---
title: "04 — Preview locally"
---

From the repo root:

```bash
cd /Users/narslan/Desktop/nebula/blog
npm ci
npx quartz build --serve -d posts
```

Then open `http://localhost:8090`.

## Stop the server

Press `Ctrl+C` in the terminal where Quartz is running.

Next: [[05-publish-to-github]]

